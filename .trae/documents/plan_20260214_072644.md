# Refactor Billing & Staking Page Layout

## UI/UX Enhancements
1.  **Standardize Page Structure**: Remove redundant background colors (`bg-[#0B0E14]`) and full-height wrappers to allow the page to inherit the base `MainLayout` styling (`bg-[#030303]`).
2.  **Unified Header Design**: Refactor the page header into a clean, single-row layout where the title is on the left and navigation tabs are on the right in a modern pill-shaped container.
3.  **Clean Navigation**: Style tabs with a subtle, professional look (e.g., subtle backgrounds for active states) instead of heavy borders, matching the "FlexIA SAAS" aesthetic.
4.  **Optimized Staking Dashboard**: Re-layout the `StakingDashboard` card to match the reference image:
    *   Left side: Main heading, description, and "Islamic Compliant" badge.
    *   Right side: Prominent "Total Credit Generated" display.
    *   Bottom: Clean grid for Portfolio Allocation and Yield History.
5.  **Polished Active Positions**: Simplify the "Active Positions" list with a more compact, list-based design that feels integrated into the dashboard.

## Technical Implementation
### Billing Page ([page.tsx](file:///c:/Users/mrcha/OneDrive/Desktop/APP_DEV/FlexAi/flexia-control-center/src/app/billing/page.tsx))
*   Remove hardcoded background and font settings.
*   Update the `header` section to use a flex layout with `justify-between`.
*   Refactor the tab mapping to use a pill-container style.

### Staking Dashboard ([StakingDashboard.tsx](file:///c:/Users/mrcha/OneDrive/Desktop/APP_DEV/FlexAi/flexia-control-center/src/components/billing/StakingDashboard.tsx))
*   Update the internal layout of the first `GlassCard` to use a 2-column flex or grid for the header.
*   Refine the "Active Positions" mapping to use a more streamlined UI.

### Staking Form ([StakingForm.tsx](file:///c:/Users/mrcha/OneDrive/Desktop/APP_DEV/FlexAi/flexia-control-center/src/components/billing/StakingForm.tsx))
*   Ensure the form card is consistently styled with the dashboard.
*   Polish the "Confirm Stake" button and asset selector buttons.

## Milestone
*   Phase 1: Refactor `page.tsx` wrapper and header.
*   Phase 2: Update `StakingDashboard.tsx` layout and "Active Positions".
*   Phase 3: Final polish on `StakingForm.tsx` and spacing across the page.

Would you like me to proceed with these structural and stylistic changes, Dear Master?