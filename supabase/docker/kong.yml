_format_version: "1.1"

services:
  - name: auth
    url: http://auth:9999
    routes:
      - paths:
          - /auth/v1

  - name: rest
    url: http://rest:3000
    routes:
      - paths:
          - /rest/v1
